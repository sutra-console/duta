# Common config — kept native_sim-safe (the CI build-check). USB/CDC and other
# board-specific bits live in boards/<board>.conf so they only apply on hardware.
CONFIG_PRINTK=y
CONFIG_GPIO=y
CONFIG_SERIAL=y
CONFIG_REBOOT=y
